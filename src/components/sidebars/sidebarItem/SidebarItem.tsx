import s from './SidebarItem.module.scss'

export type SlideBarItemProps = {
  callback: () => void
  iconUrl: string
  title: string
}
export const SidebarItem = (props: SlideBarItemProps) => {
  return <div></div>
}
